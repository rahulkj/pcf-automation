#@ load("@ytt:data", "data")
#@ load("@ytt:template", "template")

vars_folder: vars
apply_changes_config_filename: #@ data.values.apply_changes_config_filename

#@ for product in data.values.products:
_: #@ template.replace({product.name + "_config_path": "config/" + product.name})
_: #@ template.replace({product.name + "_config_filename": "config.yml"})
_: #@ template.replace({product.name + "_errand_file": "errands/" + product.name})
_: #@ template.replace({product.name + "_vars_path": "vars/" + product.name})
_: #@ template.replace({product.name + "_var_filename": "vars.yml"})
#@ end
